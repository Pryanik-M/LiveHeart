# ❤️ LiveHeart

LiveHeart — это Django-веб-приложение для управления пациентами и пользовательской аутентификации с поддержкой двухфакторной авторизации (TOTP).

Проект предназначен для демонстрации архитектуры Django-приложения с разделением логики на модули, безопасной авторизацией и удобным пользовательским интерфейсом.

---

# 🚀 Возможности проекта

## 🔐 Аутентификация
- Регистрация и вход
- Профиль пользователя
- Поддержка TOTP (двухфакторная аутентификация)
- Включение / отключение 2FA

## 👨‍⚕️ Управление пациентами
- Добавление новых пациентов
- Просмотр истории
- Работа с медицинскими данными
- Генерация PDF-отчётов

## 🎨 Frontend
- Кастомный CSS
- Структурированная статика
- Отдельные шаблоны для каждого модуля

---

Проект разделён на два приложения:

- `accounts` — отвечает за пользователей, вход и TOTP
- `patients` — отвечает за пациентов и медицинскую часть

---

# 🛠 Требования

- Python 3.10+
- pip
- Git

---

# ⚙️ Полная установка проекта (с нуля)

## 1️⃣ Клонировать репозиторий
```bash
git clone https://github.com/Pryanik-M/LiveHeart.git
cd LiveHeart
```

## 2️⃣ Создать виртуальное окружение
```bash
python -m venv venv
```

3️⃣ Активировать виртуальное окружение
Windows PowerShell
```bash
.\venv\Scripts\Activate
```
Linux / macOS
```bash
source venv/bin/activate
```
После активации перед строкой терминала появится (venv).

4️⃣ Установить зависимости
```bash
pip install -r requirements.txt
```
5️⃣ Создать файл окружения .env
В папке liveheart/liveheart/ создать файл .env:
```bash
SECRET_KEY=your-secret-key
DEBUG=True
ALLOWED_HOSTS=127.0.0.1,localhost
```
Чтобы сгенерировать SECRET_KEY:
```bash
python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
```
6️⃣ Применить миграции
```bash
python manage.py makemigrations
python manage.py migrate
```
7️⃣ Создать суперпользователя
```bash
python manage.py createsuperuser
```
8️⃣ Запустить сервер
```bash
python manage.py runserver
```
Открыть в браузере:
```bash
http://127.0.0.1:8000/
```
